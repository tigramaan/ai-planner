"use client"; import { FormEvent,useEffect,useState } from "react";import { Shell } from "@/components/Shell";import { api } from "@/lib/api";
type Task={id:string;title:string;status:string}; export default function Tasks(){const [items,setItems]=useState<Task[]>([]);const [error,setError]=useState("");useEffect(()=>{api<Task[]>("/tasks").then(setItems).catch(e=>setError(e.message));},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const title=String(new FormData(form).get("title"));try{const item=await api<Task>("/tasks",{method:"POST",body:JSON.stringify({title})});setItems(v=>[item,...v]);form.reset();}catch(err){setError(err instanceof Error?err.message:"Ошибка");}}return <Shell><header className="pageHead"><div><h1>Задачи</h1><p className="muted">Личные дела без внешних приглашений.</p></div></header><section className="panel stack"><form className="row" onSubmit={add}><label className="label" style={{flex:1}}>Новая задача<input className="field" name="title" required maxLength={300}/></label><button className="button">Добавить</button></form>{error&&<p className="error">{error}</p>}<div className="timeline">{items.length===0?<p className="muted">Задач пока нет.</p>:items.map(x=><div className="timelineItem row" key={x.id}><span>{x.title}</span><span className="status">{x.status}</span></div>)}</div></section></Shell>}
