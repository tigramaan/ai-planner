# Web PWA service

Protected responsive family interface for chat, voice, Today, integrations and password settings. Each signed-in user sees only their own data and OAuth connections. Health: `GET /health`. Configuration: `NEXT_PUBLIC_API_BASE_URL` (same-origin `/api` by default).

Run `npm install && npm run dev`. Production: `npm run build && npm start`. Tests: `npm test`.
