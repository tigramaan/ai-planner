# ADR-0003: Staged agent orchestration

The agent pipeline is transcription, typed extraction, deterministic temporal normalization, entity resolution, policy check, execution, verification and response. No single prompt receives direct network or credential access. Tool inputs and outputs are schema validated.
